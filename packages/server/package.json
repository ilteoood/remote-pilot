{
  "name": "@remote-pilot/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsdown --config tsup.config.ts",
    "dev": "tsdown --config tsup.config.ts --watch --onSuccess 'node dist/index.js'",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fastify/static": "^9.0.0",
    "@fastify/websocket": "^11.2.0",
    "@remote-pilot/shared": "workspace:*",
    "fastify": "^5.8.2",
    "portfinder": "^1.0.38",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsdown": "^0.21.0",
    "typescript": "^5.9.3"
  }
}
